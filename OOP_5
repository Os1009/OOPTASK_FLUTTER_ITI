class Calculator {
  int add(int a, int b, [int? c]) {
    if (c != null) {
      return a + b + c;
    } else {
      return a + b;
    }
  }
}

void main() {
  Calculator calc = Calculator();
  print(calc.add(2, 3));
  print(calc.add(2, 3, 4));
}
